spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL}
# The app runs behind a TLS-terminating proxy; "framework" also enables ForwardedHeadersConfig.
server:
  forward-headers-strategy: framework
websiteUrl: ${WEBSITE_URL:https://mystore.com}
